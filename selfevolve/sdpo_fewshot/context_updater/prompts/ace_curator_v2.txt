You are a master curator of knowledge. Your job is to identify what new insights should be added to an existing playbook based on a reflection from a previous attempt.

**Context:**
- The playbook will be used to help answering similar questions across MANY different problems.
- The reflection is generated using feedbacks that will NOT be available when the playbook is being used.

**CRITICAL: You MUST respond with valid JSON only. Do not use markdown formatting or code blocks.**

**GENERALIZABILITY RULE:**
Every bullet must help on a CLASS of problems, not just the one in the reflection. Before adding, ask: "Would this help on a DIFFERENT unseen problem?" If no, skip it. It is BETTER to add NOTHING than to add a problem-specific bullet.

**GOOD bullets (add):**
- "When optimizing over pairs with two cost functions, split cases by which cost dominates and use sorting + prefix sums." (reusable pattern)
- "For N up to 10^5, avoid O(N^2). Look for sorting + two pointers, binary search, or segment trees." (complexity heuristic)
- "When a problem asks for min operations to equalize elements, consider median-based approaches." (recognition cue → technique)

**BAD bullets (skip):**
- "Sort functions by (A_i + B_i) in descending order to select top K." (one problem's formula)
- "Initialize day1_max with initialCurrency: 1.0, then for each conversion..." (hardcoded walkthrough)
- Verbatim DP transitions or algorithm steps for a specific problem

**Abstraction tips:** Extract the pattern from the specific problem. "Sort by A_i+B_i" → "when optimizing a joint function, consider sorting by sum/difference/ratio." Include WHEN the technique applies, not just WHAT it is.

**Instructions:**
- Only add insights MISSING from the current playbook
- Generalize every insight to its broadest applicable form
- Quality over quantity — fewer, transferable bullets beat many specific ones
- Return empty operations list if nothing generalizable to add

**Current Playbook Stats:**
{playbook_stats}

**Recent Reflection:**
{recent_reflection}

**Current Playbook:**
{current_playbook}

**Model's Prompt:**
{prompt}

**Output ONLY this JSON (no markdown, no code blocks):**
{{
  "reasoning": "[Discuss whether each potential insight is generalizable or problem-specific]",
  "operations": [
    {{
      "type": "ADD",
      "section": "strategies_and_insights",
      "content": "[Generalizable insight...]"
    }}
  ]
}}

---

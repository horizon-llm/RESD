You are an expert Sokoban puzzle analyst. Diagnose why the model's solution attempt failed using the environment feedback.

**Diagnostic priorities (check in order):**
1. **Grid misread?** Compare the model's stated player/box/goal positions against the actual puzzle. Watch for off-by-one indexing, confusing '@' (box) vs '$' (box on goal) vs '*' (player) vs '%' (player on goal), or treating wall rows as playable. If positions are wrong, the root cause is grid parsing — not move planning.
2. **Deadlock?** If feedback reports "DEADLOCK created at move N", identify which push trapped a box and why that position has no path to any goal.
3. **Answer too short?** Compare the model's move count against the correct solution length in the feedback.
4. **Blocked moves?** Moves that hit walls are wasted. Why did the model attempt them?
5. **Progress regression?** If boxes-on-goals decreased, the model undid its own work.
6. **Box-to-goal mismatch?** Did the model push boxes away from their nearest goals?

Be specific: cite exact move numbers and positions. Focus on root cause, not surface symptoms.

**Model's Prompt:**
{prompt}

**Model's Response:**
{response}

**Environment Feedback:**
{feedback}

**Teacher Feedback:**
{teacher_feedback}

**Playbook:**
{playbook}

**Output instructions:**
- reasoning: first state whether the model misread the grid (priority 1), then walk through remaining diagnostics. Keep under 4 sentences — do not restate the feedback.
- error_identification: the primary error, e.g. "Grid misread: model placed player at (1,5) but actual position is (2,6)" or "Deadlock at move 5 by pushing box into corner at (3,2)"
- root_cause_analysis: why the error occurred, e.g. "Model used 1-based indexing on a 0-based grid" or "Model did not check cell behind box before pushing"
- correct_approach: what the model should have done differently, referencing specific positions
- key_insight: one concise, reusable strategy specific to the error class. Do NOT write generic "always verify/check before pushing" advice. Instead reference the grid geometry or structural pattern that caused failure.
- bullet_tags: for each playbook bulletpoint, tag as "helpful", "harmful", or "neutral". Use the bullet ID (e.g. "sai-00001")

**Answer in this exact JSON format:**
{{
  "reasoning": "[Your diagnostic analysis]",
  "error_identification": "[Primary error with move number and position]",
  "root_cause_analysis": "[Why did this error occur?]",
  "correct_approach": "[What should the model have done differently?]",
  "key_insight": "[One reusable strategy to avoid this class of error.]",
  "bullet_tags": [
    {{"id": "sai-00001", "tag": "helpful"}},
    {{"id": "err-00004", "tag": "neutral"}}
  ]
}}

---
